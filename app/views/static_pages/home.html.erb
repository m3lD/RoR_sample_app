<% if logged_in? %>
    <div class="row">
        <aside class="col-md-4">
            <section class="user_info">
                <%= render 'shared/user_info' %>
            </section>
            <section class="micropost_form">
                <%= render 'shared/micropost_form' %>
            </section>
        </aside>
        <div class="col-md-8">
            <h3>Inicio</h3>
            <%= render 'shared/feed' %>
        </div>
    </div>
<% else %>
    
    <div class="center jumbotron">
      <h1>Bienvenido a Tweety App</h1>

      <h2>
        Esta es la página principal de Tweety App, un clon del servicio de microbloging mas popular. 
      </h2>

      <%= link_to "Registrarse ahora!", signup_path, class: "btn btn-lg btn-primary" %>
    </div>

<% end %>   